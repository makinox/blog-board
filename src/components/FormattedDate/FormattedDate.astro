---
interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time class="capitalize" datetime={date.toISOString()}>
  {
    date.toLocaleDateString("es-MX", {
      month: "short",
      day: "numeric",
      year: "numeric",
    })
  }
</time>
